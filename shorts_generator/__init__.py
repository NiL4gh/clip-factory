# Pipeline removed in Phase 4 — server/main.py imports modules directly.
# This package is now a collection of individual modules:
#   config, downloader, transcriber, highlights, clipper, enhancer, cache, logger
