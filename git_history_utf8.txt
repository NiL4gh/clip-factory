commit a78485d205bdcd70462003035912328d6659fdd8
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 04:07:43 2026 +0600

    Fix n-challenge by removing cookies to enforce mobile clients

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 8fdfbdc..4bb4f12 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -16,11 +16,11 @@ def download_video(url, work_dir, cookie_path=None):
         "-f", "bestvideo[height<=720]+bestaudio/best[height<=720]/bestvideo+bestaudio/best",
         "-o", f"{work_dir}/source.%(ext)s",
         "--merge-output-format", "mp4",
+        "--extractor-args", "youtube:player_client=ios,android",
         "--impersonate", "chrome",
         url
     ]
-    if cookie_path and os.path.exists(cookie_path):
-        cmd.extend(["--cookies", cookie_path])
+
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")
     try:

commit 37fd487873a7af1bc2056d89c950a6dec507322e
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 03:57:47 2026 +0600

    Completely rewrite yt-dlp call using subprocess CLI to ensure robust bot bypass

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index f68f29a..8fdfbdc 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -1,4 +1,3 @@
-import yt_dlp
 import subprocess
 import os
 import glob
@@ -12,25 +11,23 @@ def download_video(url, work_dir, cookie_path=None):
     for f in glob.glob(f"{work_dir}/source.*"):
         os.remove(f)
 
-    ydl_opts = {
-        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/bestvideo+bestaudio/best",
-        "outtmpl": f"{work_dir}/source.%(ext)s",
-        "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
-        "quiet": False,
-        "ignoreerrors": False,
-        "socket_timeout": 60,
-        "merge_output_format": "mp4",
-        "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "extractor_args": {"youtube": {"player_client": ["ios", "android"]}},
-    }
+    cmd = [
+        "yt-dlp",
+        "-f", "bestvideo[height<=720]+bestaudio/best[height<=720]/bestvideo+bestaudio/best",
+        "-o", f"{work_dir}/source.%(ext)s",
+        "--merge-output-format", "mp4",
+        "--impersonate", "chrome",
+        url
+    ]
+    if cookie_path and os.path.exists(cookie_path):
+        cmd.extend(["--cookies", cookie_path])
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")
     try:
-        with yt_dlp.YoutubeDL(ydl_opts) as ydl:
-            ydl.download([url])
-    except Exception as e:
-        ui_logger.log(f"yt-dlp failed: {e}")
-        raise
+        subprocess.run(cmd, check=True, capture_output=True, text=True)
+    except subprocess.CalledProcessError as e:
+        ui_logger.log(f"yt-dlp failed: {e.stderr}")
+        raise RuntimeError(f"yt-dlp failed: {e.stderr}")
 
     # Fast remux fallback ΓÇö no re-encoding
     if not os.path.exists(output_mp4):

commit e7ce034c031799af9eb0ca2eeb67a6a778b53de2
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 03:51:03 2026 +0600

    Fix AssertionError by removing impersonate arg

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index fa7165c..f68f29a 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -22,7 +22,6 @@ def download_video(url, work_dir, cookie_path=None):
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
         "extractor_args": {"youtube": {"player_client": ["ios", "android"]}},
-        "impersonate": "chrome",
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit b33f00f0579717eac0c29246e3f58a13cf28ba1f
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 03:41:51 2026 +0600

    Fix YouTube downloader and embed cookies

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 7b4d8c0..fa7165c 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -13,7 +13,7 @@ def download_video(url, work_dir, cookie_path=None):
         os.remove(f)
 
     ydl_opts = {
-        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/best",
+        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/bestvideo+bestaudio/best",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
         "quiet": False,
@@ -21,6 +21,8 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
+        "extractor_args": {"youtube": {"player_client": ["ios", "android"]}},
+        "impersonate": "chrome",
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit 9889d65608eceeb099be8fe6803ef6d1d9bb7a19
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 02:26:28 2026 +0600

    Fix yt-dlp n-challenge with curl-cffi and natural client fallback

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 69c95f0..7b4d8c0 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -21,7 +21,6 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "extractor_args": {"youtube": ["player_client=ios,android,web"]},
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit 1fbf835c20436af3d47d6dc2e29a1480fe83cdc1
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 02:12:19 2026 +0600

    Fix yt-dlp format strictness error

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index c88ca75..69c95f0 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -13,7 +13,7 @@ def download_video(url, work_dir, cookie_path=None):
         os.remove(f)
 
     ydl_opts = {
-        "format": "bestvideo[height<=720][ext=mp4]+bestaudio[ext=m4a]/best[height<=720][ext=mp4]/best",
+        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/best",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
         "quiet": False,
@@ -21,7 +21,7 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "extractor_args": {"youtube": ["player_client=android,ios", "player_skip=webpage,configs"]},
+        "extractor_args": {"youtube": ["player_client=ios,android,web"]},
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit 5a32345d748b0ddfbdf9538c2308d9e190114b63
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 01:42:08 2026 +0600

    Aggressive yt-dlp bot protection bypass

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 163fb09..c88ca75 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -13,8 +13,7 @@ def download_video(url, work_dir, cookie_path=None):
         os.remove(f)
 
     ydl_opts = {
-        # Select best video up to 720p (saves bandwidth/time) + best audio, or best available
-        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/best",
+        "format": "bestvideo[height<=720][ext=mp4]+bestaudio[ext=m4a]/best[height<=720][ext=mp4]/best",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
         "quiet": False,
@@ -22,7 +21,7 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "extractor_args": {"youtube": ["player_client=ios,android"]},
+        "extractor_args": {"youtube": ["player_client=android,ios", "player_skip=webpage,configs"]},
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit a2df2de906a3031ccf9201612d4deab8267ae719
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 01:35:45 2026 +0600

    Fix yt-dlp challenge error & Soft Light Theme UI

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 8f57b8b..163fb09 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -22,7 +22,7 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "extractor_args": {"youtube": ["player_client=web"]},
+        "extractor_args": {"youtube": ["player_client=ios,android"]},
     }
 
     ui_logger.log("yt-dlp: Fetching remote components and downloading...")

commit 53139f8d22703c57283436d7332705bea8822449
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Wed May 6 01:21:45 2026 +0600

    UI Overhaul: Slate theme + Editor layout + yt-dlp fix

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index ac3cc8e..8f57b8b 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -13,7 +13,8 @@ def download_video(url, work_dir, cookie_path=None):
         os.remove(f)
 
     ydl_opts = {
-        "format": "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b",
+        # Select best video up to 720p (saves bandwidth/time) + best audio, or best available
+        "format": "bestvideo[height<=720]+bestaudio/best[height<=720]/best",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
         "quiet": False,
@@ -21,7 +22,6 @@ def download_video(url, work_dir, cookie_path=None):
         "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "remote_components": ["ejs:github"],
         "extractor_args": {"youtube": ["player_client=web"]},
     }
 

commit 1799b38f2f139a2efb59dbe677a093ffa9974b91
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Mon May 4 01:29:53 2026 +0600

    feat(downloader): integrate UIStreamLogger

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 1a2f23e..ac3cc8e 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -2,11 +2,12 @@ import yt_dlp
 import subprocess
 import os
 import glob
-
+from .logger import ui_logger
 
 def download_video(url, work_dir, cookie_path=None):
     output_mp4 = f"{work_dir}/source.mp4"
 
+    ui_logger.log(f"Starting download for: {url}")
     # Clean old source files to prevent conflicts
     for f in glob.glob(f"{work_dir}/source.*"):
         os.remove(f)
@@ -15,21 +16,21 @@ def download_video(url, work_dir, cookie_path=None):
         "format": "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
-        "quiet": False,  # Changed from True to see errors (Bug 12)
+        "quiet": False,
         "ignoreerrors": False,
-        "socket_timeout": 60,  # Prevent indefinite hangs (Bug 20)
+        "socket_timeout": 60,
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
-        "remote_components": ["ejs:github"],  # Fix for YouTube n-sig challenge
+        "remote_components": ["ejs:github"],
         "extractor_args": {"youtube": ["player_client=web"]},
     }
 
-    print(f"  [downloader] Downloading {url}...")
+    ui_logger.log("yt-dlp: Fetching remote components and downloading...")
     try:
         with yt_dlp.YoutubeDL(ydl_opts) as ydl:
             ydl.download([url])
     except Exception as e:
-        print(f"  [downloader] yt-dlp failed: {e}")
+        ui_logger.log(f"yt-dlp failed: {e}")
         raise
 
     # Fast remux fallback ΓÇö no re-encoding
@@ -37,8 +38,10 @@ def download_video(url, work_dir, cookie_path=None):
         files = glob.glob(f"{work_dir}/source.*")
         if files:
             raw = files[0]
+            ui_logger.log("Fast remuxing raw file to mp4 format...")
             subprocess.run(["ffmpeg", "-y", "-i", raw, "-c", "copy", output_mp4], check=True)
             if raw != output_mp4:
                 os.remove(raw)
 
+    ui_logger.log("Download complete.")
     return output_mp4

commit 253fd43f64eb4d28841e373b1aa7bb1e9e49a32f
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Mon May 4 01:25:15 2026 +0600

    fix(downloader): add JS challenge solver and fix silent yt-dlp failures

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 63adffe..1a2f23e 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -15,13 +15,22 @@ def download_video(url, work_dir, cookie_path=None):
         "format": "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b",
         "outtmpl": f"{work_dir}/source.%(ext)s",
         "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
-        "quiet": True,
+        "quiet": False,  # Changed from True to see errors (Bug 12)
+        "ignoreerrors": False,
+        "socket_timeout": 60,  # Prevent indefinite hangs (Bug 20)
         "merge_output_format": "mp4",
         "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
+        "remote_components": ["ejs:github"],  # Fix for YouTube n-sig challenge
+        "extractor_args": {"youtube": ["player_client=web"]},
     }
 
-    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
-        ydl.download([url])
+    print(f"  [downloader] Downloading {url}...")
+    try:
+        with yt_dlp.YoutubeDL(ydl_opts) as ydl:
+            ydl.download([url])
+    except Exception as e:
+        print(f"  [downloader] yt-dlp failed: {e}")
+        raise
 
     # Fast remux fallback ΓÇö no re-encoding
     if not os.path.exists(output_mp4):

commit d01b3caabd3128455d9f337ebffd3108888e1243
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Sun May 3 00:53:47 2026 +0600

    Refactor video download function for clarity

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 51186c1..63adffe 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -3,28 +3,33 @@ import subprocess
 import os
 import glob
 
+
 def download_video(url, work_dir, cookie_path=None):
     output_mp4 = f"{work_dir}/source.mp4"
-    
-    # Cascade: Best MP4 -> Best Video+Audio -> Best Single File
+
+    # Clean old source files to prevent conflicts
+    for f in glob.glob(f"{work_dir}/source.*"):
+        os.remove(f)
+
     ydl_opts = {
-        'format': 'bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b',
-        'outtmpl': f'{work_dir}/source.%(ext)s',
-        'cookiefile': cookie_path if cookie_path and os.path.exists(cookie_path) else None,
-        'quiet': True,
-        'merge_output_format': 'mp4',
-        'postprocessors': [{'key': 'FFmpegVideoConvertor', 'preferedformat': 'mp4'}],
+        "format": "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b",
+        "outtmpl": f"{work_dir}/source.%(ext)s",
+        "cookiefile": cookie_path if cookie_path and os.path.exists(cookie_path) else None,
+        "quiet": True,
+        "merge_output_format": "mp4",
+        "postprocessors": [{"key": "FFmpegVideoConvertor", "preferedformat": "mp4"}],
     }
 
     with yt_dlp.YoutubeDL(ydl_opts) as ydl:
         ydl.download([url])
 
-    # Nuclear Fallback: Fast Remuxing (no re-encoding)
+    # Fast remux fallback ΓÇö no re-encoding
     if not os.path.exists(output_mp4):
         files = glob.glob(f"{work_dir}/source.*")
         if files:
-            raw_file = files[0]
-            subprocess.run(['ffmpeg', '-y', '-i', raw_file, '-c', 'copy', output_mp4], check=True)
-            if raw_file != output_mp4: os.remove(raw_file)
-            
+            raw = files[0]
+            subprocess.run(["ffmpeg", "-y", "-i", raw, "-c", "copy", output_mp4], check=True)
+            if raw != output_mp4:
+                os.remove(raw)
+
     return output_mp4

commit 34da259a9bd9753faf52595a9407f9aa9cf63cf9
Author: NiL4gh <153031890+NiL4gh@users.noreply.github.com>
Date:   Sat May 2 23:02:06 2026 +0600

    Update downloader.py

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index cd84e11..51186c1 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -1,36 +1,30 @@
-"""YouTube source video download via MuAPI /youtube-download."""
-from typing import Dict
+import yt_dlp
+import subprocess
+import os
+import glob
 
-from . import muapi
+def download_video(url, work_dir, cookie_path=None):
+    output_mp4 = f"{work_dir}/source.mp4"
+    
+    # Cascade: Best MP4 -> Best Video+Audio -> Best Single File
+    ydl_opts = {
+        'format': 'bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/bv*+ba/b',
+        'outtmpl': f'{work_dir}/source.%(ext)s',
+        'cookiefile': cookie_path if cookie_path and os.path.exists(cookie_path) else None,
+        'quiet': True,
+        'merge_output_format': 'mp4',
+        'postprocessors': [{'key': 'FFmpegVideoConvertor', 'preferedformat': 'mp4'}],
+    }
 
+    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
+        ydl.download([url])
 
-def _extract_video_url(result: Dict) -> str:
-    """MuAPI result shapes vary by endpoint ΓÇö try common keys."""
-    for key in ("video_url", "url", "output_url", "result_url"):
-        v = result.get(key)
-        if isinstance(v, str) and v.startswith("http"):
-            return v
-
-    output = result.get("outputs") or result.get("output") or result.get("result") or {}
-    if isinstance(output, dict):
-        for key in ("video_url", "url", "output_url"):
-            v = output.get(key)
-            if isinstance(v, str) and v.startswith("http"):
-                return v
-    if isinstance(output, list) and output and isinstance(output[0], str) and output[0].startswith("http"):
-        return output[0]
-
-    raise RuntimeError(f"Could not find downloaded video URL in MuAPI response: {result}")
-
-
-def download_youtube(video_url: str, fmt: str = "720") -> str:
-    """Hand a YouTube URL to MuAPI; return a hosted mp4 URL we can read from."""
-    print(f"[download] requesting {video_url} @ {fmt}p", flush=True)
-    result = muapi.run(
-        "youtube-download",
-        {"video_url": video_url, "format": fmt},
-        label="youtube-download",
-    )
-    out = _extract_video_url(result)
-    print(f"[download] ready: {out}", flush=True)
-    return out
+    # Nuclear Fallback: Fast Remuxing (no re-encoding)
+    if not os.path.exists(output_mp4):
+        files = glob.glob(f"{work_dir}/source.*")
+        if files:
+            raw_file = files[0]
+            subprocess.run(['ffmpeg', '-y', '-i', raw_file, '-c', 'copy', output_mp4], check=True)
+            if raw_file != output_mp4: os.remove(raw_file)
+            
+    return output_mp4

commit 8cf66ba90da18d614936af56be935e1363554263
Author: Anil Matcha <matcha72@gmail.com>
Date:   Tue Apr 28 18:46:38 2026 +0530

    Fix MuAPI integration: gpt-5-4 ΓåÆ gpt-5-mini, outputs key, timeouts
    
    - downloader: read URL from `outputs` (the actual MuAPI response key)
    - highlights: switch to gpt-5-mini; gpt-5-4 currently fails server-side
      with "Unknown error" on every request
    - muapi: bump submit/poll HTTP timeouts and retry transient network errors

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 5037e51..cd84e11 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -11,16 +11,7 @@ def _extract_video_url(result: Dict) -> str:
         if isinstance(v, str) and v.startswith("http"):
             return v
 
-    outputs = result.get("outputs")
-    if isinstance(outputs, list) and outputs and isinstance(outputs[0], str) and outputs[0].startswith("http"):
-        return outputs[0]
-    if isinstance(outputs, dict):
-        for key in ("video_url", "url", "output_url"):
-            v = outputs.get(key)
-            if isinstance(v, str) and v.startswith("http"):
-                return v
-
-    output = result.get("output") or result.get("result") or {}
+    output = result.get("outputs") or result.get("output") or result.get("result") or {}
     if isinstance(output, dict):
         for key in ("video_url", "url", "output_url"):
             v = output.get(key)

commit 47b86746d05046d0453688356648d599a0379e25
Author: Anil Matcha <matcha72@gmail.com>
Date:   Mon Apr 27 18:57:44 2026 +0530

    fix: handle 'outputs' list in MuAPI response
    
    MuAPI returns the rendered URL under top-level 'outputs' (plural list),
    but _extract_video_url only checked 'output'/'result' (singular),
    causing youtube-download and autocrop to fail with
    'Could not find downloaded video URL in MuAPI response'.

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
index 7462763..5037e51 100644
--- a/shorts_generator/downloader.py
+++ b/shorts_generator/downloader.py
@@ -11,6 +11,15 @@ def _extract_video_url(result: Dict) -> str:
         if isinstance(v, str) and v.startswith("http"):
             return v
 
+    outputs = result.get("outputs")
+    if isinstance(outputs, list) and outputs and isinstance(outputs[0], str) and outputs[0].startswith("http"):
+        return outputs[0]
+    if isinstance(outputs, dict):
+        for key in ("video_url", "url", "output_url"):
+            v = outputs.get(key)
+            if isinstance(v, str) and v.startswith("http"):
+                return v
+
     output = result.get("output") or result.get("result") or {}
     if isinstance(output, dict):
         for key in ("video_url", "url", "output_url"):

commit 282f2cb9bbe6387d25c79166fbf5b0db742f851e
Author: Anil Matcha <matcha72@gmail.com>
Date:   Sun Apr 26 07:09:24 2026 +0530

    Initial commit: AI YouTube Shorts Generator (MuAPI edition)
    
    Open-source Opus Clip alternative. Pipeline:
    YouTube URL -> MuAPI /youtube-download -> local Whisper transcript
                -> MuAPI /gpt-5-4 highlights -> MuAPI /autocrop clips.
    
    Highlight/virality logic ported from ViralVadoo's transcript_analysis,
    rewired to call MuAPI gpt-5-4 instead of OpenAI directly. Includes
    content-type detection, long-video chunking, and overlap dedupe.
    
    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

diff --git a/shorts_generator/downloader.py b/shorts_generator/downloader.py
new file mode 100644
index 0000000..7462763
--- /dev/null
+++ b/shorts_generator/downloader.py
@@ -0,0 +1,36 @@
+"""YouTube source video download via MuAPI /youtube-download."""
+from typing import Dict
+
+from . import muapi
+
+
+def _extract_video_url(result: Dict) -> str:
+    """MuAPI result shapes vary by endpoint ΓÇö try common keys."""
+    for key in ("video_url", "url", "output_url", "result_url"):
+        v = result.get(key)
+        if isinstance(v, str) and v.startswith("http"):
+            return v
+
+    output = result.get("output") or result.get("result") or {}
+    if isinstance(output, dict):
+        for key in ("video_url", "url", "output_url"):
+            v = output.get(key)
+            if isinstance(v, str) and v.startswith("http"):
+                return v
+    if isinstance(output, list) and output and isinstance(output[0], str) and output[0].startswith("http"):
+        return output[0]
+
+    raise RuntimeError(f"Could not find downloaded video URL in MuAPI response: {result}")
+
+
+def download_youtube(video_url: str, fmt: str = "720") -> str:
+    """Hand a YouTube URL to MuAPI; return a hosted mp4 URL we can read from."""
+    print(f"[download] requesting {video_url} @ {fmt}p", flush=True)
+    result = muapi.run(
+        "youtube-download",
+        {"video_url": video_url, "format": fmt},
+        label="youtube-download",
+    )
+    out = _extract_video_url(result)
+    print(f"[download] ready: {out}", flush=True)
+    return out
