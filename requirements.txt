# Core
requests>=2.31
python-dotenv>=1.0
yt-dlp
curl-cffi>=0.5.10
huggingface-hub>=0.20

# UI
gradio>=4.0

# Video
opencv-python-headless>=4.8

# Transcription (GPU-accelerated)
faster-whisper>=1.0
torch>=2.0

# Media fetching (B-Roll images via DuckDuckGo)
duckduckgo-search>=5.0

# Face tracking (replaces legacy Haar Cascades)
mediapipe>=0.10

# LLM — installed separately in setup.sh with CUDA flags
# llama-cpp-python
# Audio Analysis
librosa>=0.10

# Backend API
fastapi>=0.100.0
uvicorn>=0.23.0
